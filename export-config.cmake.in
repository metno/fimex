get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" DIRECTORY)
include(${SELF_DIR}/@EXPORTNAME@@MINUS_FIMEX_VERSION@-targets.cmake)

if("@EXPORTNAME@" STREQUAL "fimex")
  get_filename_component(FIMEX_INSTALLED_IO_PLUGINS_PATH "${SELF_DIR}" PATH)
  get_filename_component(FIMEX_INSTALLED_IO_PLUGINS_PATH "${FIMEX_INSTALLED_IO_PLUGINS_PATH}" PATH)
  set_property(TARGET libfimex PROPERTY INSTALLED_IO_PLUGINS_PATH "${FIMEX_INSTALLED_IO_PLUGINS_PATH}")
endif()
